# ---------------------------------------------------------------------------
# StockIQ India - Python dependencies
# ---------------------------------------------------------------------------
# Install these with:   pip install -r requirements.txt
#
# Everything listed here is PURE PYTHON (no C compiler needed), so it installs
# quickly on Windows, macOS and Linux, including on very new Python versions.
# ---------------------------------------------------------------------------

Flask==3.1.0             # The web framework (routes, templates, sessions)
Flask-Cors==5.0.0        # Lets a separate frontend call our API if you ever split them
Flask-SQLAlchemy==3.1.1  # Thin, beginner-friendly glue between Flask and SQLAlchemy
SQLAlchemy==2.0.36       # The ORM: lets us use Python classes instead of raw SQL
python-dotenv==1.0.1     # Reads the .env file so secrets never live in the code
requests==2.32.3         # Used to call external HTTP APIs (news, market data, AI)
Werkzeug==3.1.3          # Ships with Flask; we use its secure password hashing

# ---------------------------------------------------------------------------
# OPTIONAL EXTRAS (not installed by default - see README section "Real data")
# ---------------------------------------------------------------------------
# anthropic>=0.40.0      # Official Claude SDK. We call the AI over plain HTTP
#                        # instead, so this is genuinely optional.
# yfinance>=0.2.50       # A real market-data source for NSE/BSE symbols.
#                        # Needs pandas + numpy, which may not have prebuilt
#                        # wheels for the newest Python releases yet.
# ---------------------------------------------------------------------------
